import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "AI Prompt Generator Tool - Ranksper AI Studio",
    description: "Enhance your writing journey with our Prompt Generator AI Tool. Create customized prompts that spark inspiration and elevate your creative projects.",
};

export default function PromptGeneratorLayout({ children }: { children: React.ReactNode }) {
    return children;
}
