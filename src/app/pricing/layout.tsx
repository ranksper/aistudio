import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Pricing - Ranksper AI Studio",
    description: "Explore the pricing plans and subscription options for Ranksper AI Studio.",
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
    return children;
}
