import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "SEO Specialist AI Chatbot - Ranksper AI Studio",
    description: "Personalized SEO Specialist with Highest Skills for Everything",
};

export default function SEOChatbotLayout({ children }: { children: React.ReactNode }) {
    return children;
}
