import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Content Writer AI Chatbot - Ranksper AI Studio",
    description: "Personalized Content Specialist with Highest Skills for Content Creation",
};

export default function WriterChatbotLayout({ children }: { children: React.ReactNode }) {
    return children;
}
